//! Placeholder retained for the legacy `crate::app::smtps` module
//! path. The real SMTP handler now lives in [`crate::app::mail`] and
//! is wired into the runtime via the listener spawner in
//! [`crate::srv::mail`].
